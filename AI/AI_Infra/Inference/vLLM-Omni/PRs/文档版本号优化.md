请使用 `mkdocs-macros-plugin` 插件，帮我把该项目所有的文档中，涉及 vllm 版本号的地方，统一在 `mkdocs.yml` 中进行配置，并执行动态替换。
比如，在 `mkdocs.yml` 中设置 extra 属性：
`vllm_version_cuda: v0.xx.x`
`vllm_version_rocm: v0.xx.x`
`vllm_version_xpu: v0.xx.x`
`vllm_version_npu: v0.xx.x`
然后，替换文档中所有的版本号，使其可以动态地从 `mkdocs.yml` 中解析。
比如，在文档中使用 `{{ vllm_version_xxx }}` 进行替换。
