背景：
tests/models/multimodal/generation/test_voxtral_realtime.py 这个测试在vllm的rocm ci上，由于在不同测试之间，GPU资源（显存）没有正确的清理干净，导致下一个测试在启动时就出现异常：`ValueError: Free memory on device cuda:0 (24.68/255.98 GiB) on startup is less than desired GPU memory utilization (0.9, 230.39 GiB). Decrease GPU memory utilization or reduce GPU memory used by other processes.`
需求：
请使用 VllmRunner（以 with vllm_runner）的形式，改造 test_voxtral_realtime.py 这个文件，使其能够复用 VllmRunner 中 `__exit__()` 的清理逻辑。
