# 精度测试

## 环境准备

```bash
export HF_ENDPOINT="https://hf-mirror.com"
pip install lm-eval
```

## 精度测试

```bash
export HF_ENDPOINT="https://hf-mirror.com"
VLLM_USE_MODELSCOPE=True lm_eval \
--model vllm-vlm \
--model_args pretrained=Qwen/Qwen2.5-VL-7B-Instruct,max_model_len=8192,tensor_parallel_size=2 \
--tasks mmmu_val \
--apply_chat_template \
--fewshot_as_multiturn \
--batch_size auto \
--num_fewshot 5
```

## 测试结果

```bash
```

---

报错：

```bash
```
