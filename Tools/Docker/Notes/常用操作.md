# Docker 常用操作

## 清理不用的镜像和容器

```bash
# df -h /shareddata

# 查看 docker 内存占用情况
docker system df

# 清理没有被使用的 container
docker system prune
# 清理 build cache
docker builder prune
# 删除所有没有被现有 container 使用的 image
docker image prune -a
```
