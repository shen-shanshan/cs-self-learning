请帮我创建一个 skill，名叫 vllm-multimodal-open-issue-analyzer，用于搜集并整理 https://github.com/vllm-project/vllm 项目中多模态相关的未解决的 issue。
包括：多模态相关特性（比如：vit cuda graph、EPD 分离等）、多模态模型（比如：VL/Omni/audio/whisper等）。
要求按问题类型进行分类，每一个问题类别下需要有对应 issue 的链接，并最终生成一份 markdown 格式的报告，放到当前 skill 的 `./outputs` 目录下。
